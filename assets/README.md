# Assets

This demo now reads real dataset folders such as `LADD/` directly, so `assets/` is no longer the primary source for scene data.

Use `assets/` only if you later want to add extra static imagery, logos, or documentation files that are not part of the benchmark dataset folders.
